# Backend API URL (uses backend for chat, face, documents)
VITE_API_URL=http://localhost:8000

# Chat/document Q&A are handled entirely by the backend — see backend/.env.example
# for ANTHROPIC_API_KEY. The frontend does not call Anthropic directly.
